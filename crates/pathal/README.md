# `pathal`

Path abstraction layer.

A path is an ordered list of commands used to traverse a hierarchical structure
such as a file system or website content.

For example, a POSIX path uses a leading `/` to mean "go to root" and then a
list of slash-separated link names to follow.
